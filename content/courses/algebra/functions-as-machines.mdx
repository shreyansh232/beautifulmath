---
title: "Functions as machines"
slug: "functions-as-machines"
order: 3
summary: "A function is a reliable machine: each allowed input gets exactly one output. That single rule is how models, formulas, and graphs stay coherent."
mathematician:
  name: "Leonhard Euler"
  years: "1707-1783"
  vignette: "Euler wrote mathematics the way a generous teacher speaks: clearly, abundantly, and with notation that sticks. Popularizing f(x) was not vanity. It was a tool so relationships could be named, composed, and reused. The ethic: invent language that makes hard ideas handleable, then use it everywhere."
realWorld: "Temperature converters, tax brackets (piecewise), and pricing formulas are functions: plug in an input, get a determined output."
aiMlHook: "A neural network is a giant composed function: layer after layer of maps from activations to activations. Training adjusts the parameters inside that function."
prerequisites:
  - "equations-as-relationships"
curiositySeeds:
  - "What makes something a function?"
  - "What is composition?"
estimatedMinutes: 34
furtherReading:
  - title: "MacTutor - Leonhard Euler"
    url: "https://mathshistory.st-andrews.ac.uk/Biographies/Euler/"
    kind: reference
  - title: "3Blue1Brown - Essence of calculus (functions and change)"
    url: "https://www.youtube.com/watch?v=WUvTyaaNkzM"
    kind: video
  - title: "Khan Academy - What is a function?"
    url: "https://www.khanacademy.org/math/algebra/x2f8bb11595b61c86:functions"
    kind: video
  - title: "Steven Strogatz - The Joy of X"
    url: "https://www.stevenstrogatz.com/books/the-joy-of-x"
    kind: book
    note: "Power Tools chapter: functions as transformers."
  - title: "Algebrica - Functions"
    url: "https://algebrica.org/functions/"
    kind: article
    note: "Domain, range, and function language with diagrams."
---

## First principles

A **function** $f$ from a set of inputs to outputs assigns **exactly one** output $f(x)$ to each allowed $x$.

<FunctionMachineDiagram />

If one input could yield two different outputs, it is not a function. Vertical-line tests on graphs encode the same idea.


## Slow down: one input, one output

The vertical-line test is a cartoon of the definition: if a single $x$ tried to claim two $y$ values, the machine would be indecisive. Functions are decisive maps. That decisiveness is what lets you compose $f\circ g$ without ambiguity - feed forward, as neural nets do layer after layer.

## Examples

- $f(x) = 2x + 1$ (linear)
- $g(x) = x^2$ (each $x$ still has one square; many $x$ share an output)
- Domain matters: $\sqrt{x}$ needs $x \ge 0$ in the reals

## Composition

$(f \circ g)(x) = f(g(x))$: feed $x$ into $g$, then feed that result into $f$. Order usually matters.

## Why this is the gateway to models

A formula is a function. A spreadsheet column computed from another is a function. A trained predictor $\hat{y} = f_w(x)$ is a function whose internal knobs $w$ we choose.

Euler's $f(x)$ notation makes that reusable: name the machine, then apply it.

<StoryNote>
  <p>
    In the "Power Tools" chapter of <em>The Joy of X</em>, Strogatz puts it bluntly: the function of functions is to transform. Name the machine, feed it an input, trust the output rule. That is the whole plot.
  </p>
</StoryNote>

## Real world

Unit conversion $C = \frac{5}{9}(F - 32)$ is a function from Fahrenheit to Celsius. Invertible here, so you can undo it.

## AI / ML bridge

Layers compose: $f_3(f_2(f_1(x)))$. Backpropagation is the chain rule on that composition. You will meet that formally in calculus intuition.

<StoryNote title="Curiosity from history">
  <p>
    Euler's $f(x)$ notation looks small. It is a cultural invention: name the machine, reuse it, compose it. Once functions are objects, calculus and ML can move them around like parts.
  </p>
</StoryNote>

## Dig deeper

Algebrica's "Functions" and "Composite functions" entries reward a slow read. Composition is the secret grammar of neural nets.

## Curiosity seeds

- Is a vertical line a function of $x$? Why does the vertical-line test care?
- Where do you already use composition (unit conversions chained together)?

## Carry this forward

- One input, one output.
- Composition builds complexity from simple maps.
- Next: seeing functions as graphs in the plane.
